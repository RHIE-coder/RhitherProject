import { expect } from 'chai'

// const target = import("../../src/routes/src/contract-caller");


describe('Routing(contract-caller) Test', function () {
  it('Check Routing Value', async function () {
    // expect(await math.add(10, 20)).to.be.equal(30)
  })
})